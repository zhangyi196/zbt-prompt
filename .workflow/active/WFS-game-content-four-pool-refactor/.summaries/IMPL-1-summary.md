# IMPL-1 Summary

Completed. Pilot blind-box bundles now use the four content pools: `core_items`, `support_items`, `visible_small_items`, and `scene_expansion_items`. Legacy `BLIND_BOXES` still exposes `large`, `medium`, `small`, and `hanging`.
