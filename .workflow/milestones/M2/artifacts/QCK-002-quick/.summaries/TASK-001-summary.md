# TASK-001: 重构组图 23 的 List 2 为 6 类强变化菜单

## Changes
- `prompts/2.group-image/组图 23.md`: 将 List 2 的抽象类别从原 9 类收敛为 6 类完整变化菜单。
- `prompts/2.group-image/组图 23.md`: 新增默认优先整对象级、整块内容级强变化的硬规则，并补充强变化示例。
- `prompts/2.group-image/组图 23.md`: 明确禁用边缘鼓包、轻微改形、细裂纹、小污点、轻微偏移、轻微缩放、颜色/纹理/明暗微调等弱轮廓与微痕迹变化。
- `prompts/2.group-image/组图 23.md`: 将类型轮换约束改为围绕 6 类菜单执行，并保留服装变化复用同一菜单、不独立成新类型的限制。

## Verification
- [x] 文件包含 6 类菜单精确字符串：使用 `Grep` 命中 `抽象类别只用这一串：`目标文案。
- [x] 文件包含强优先级精确字符串：使用 `Grep` 命中 `默认优先整对象级、整块内容级强变化；弱轮廓变化直接降级或禁用。`。
- [x] 文件包含弱轮廓禁令精确字符串：使用 `Grep` 命中 `严禁边缘鼓包、花瓣轻微改形、只增减一点轮廓、细裂纹、小划痕、小污点、轻微偏移、轻微缩放、颜色/纹理/明暗微调。`。
- [x] 文件包含服装复用 6 类菜单精确字符串：使用 `Grep` 命中 `服装变化不是独立类型，只能复用上述 6 类变化菜单表达。`。
- [x] 任务文件只声明修改目标提示词文件：`TASK-001.json` 的 `scope` 与 `focus_paths` 仅指向 `prompts/2.group-image/组图 23.md`。

## Tests
- [x] `Grep` 精确检查 6 类菜单字符串是否存在：通过。
- [x] `Grep` 精确检查强优先级字符串是否存在：通过。
- [x] `Grep` 精确检查弱轮廓禁令字符串是否存在：通过。
- [x] `Grep` 精确检查服装复用 6 类字符串是否存在：通过。

## Deviations
- None

## Notes
- 本次仅修改 `List 2：辅助差异` 规则段落与任务 scratch 文件，未扩散到其他提示词文件。
