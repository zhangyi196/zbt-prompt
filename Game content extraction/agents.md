# Agents Guide

访问 `Game content extraction/` 目录时，请先阅读并遵循同目录下的 `CLAUDE.md`。

`CLAUDE.md` 代替本目录的 agents 指南；本文件只作为入口提示，不重复维护规则。人物表情前置的 `具体表情` 候选数量、App 对表情库单人/多人 1-8 模板的解析，以及多候选分隔、整组校验、随机择一和去重替换的约束以 `CLAUDE.md` 为准。
