# 手作+编织 审查文档

审查日期：2026-05-08

## 审查对象

- 类别：`手作+编织`
- 数据来源：`Game content extraction/data/blind_boxes.py` 中 `BLIND_BOX_ITEM_POOL_BUNDLES['手作+编织']`
- 审查范围：仅审查该类别 `core_items` / `support_items` / `visible_small_items` / `scene_expansion_items` 四池内容
- 本次操作：已再次同步修改 `blind_boxes.py` 中该类别数据，并更新本审查文档

## 总体结论

状态：`已再次修改并复审`

这一次的主要问题已经不再是展示壳体，而是第四池里又同时出现了两种模板复写：

- `scene_expansion_items` 里同时出现 `配色图册`、`花样图册`、`编织图谱本`、`作品记录本`、`针法记录本`、`项目记录本`、`配色样册`、`编织灵感册`、`图样词典`、`行数记录本`、`钩针图册`、`棒针图册`。
- 同时还出现 `多个 xx布包`、`多个 xx工具包`、`多个 xx防尘袋` 这类同后缀家族堆叠。
- 这些名称虽然都是真实词，但变化主要落在前缀说明，稳定不变的仍是同一文书骨架，不符合“抽象职能文书族最多保留 1 个代表项”的新全局规则。
- 如果不压掉这组条目，第四池会再次从“自然中型物”滑回“说明资料池 / 记录本池”。

## 本次修改方向

- `core_items`：本轮未动，暂时接受现状。
- `support_items`：本轮未动，保持上轮结果。
- `visible_small_items`：本轮未动，保持现状。
- `scene_expansion_items`：把抽象职能文书族压到只保留 `图样词典` 1 个代表项，同时把 `布包 / 工具包 / 防尘袋 / 布袋` 这类同后缀家族压到单个代表项，其余全部替换成更具体的设备、收纳物、防尘物和卷筒物。

## 本次主要压掉的问题

- `记录本 / 图册 / 图谱本 / 样册 / 灵感册` 的抽象职能文书链。
- 只靠前缀差异成立的“前缀换词、后缀不变”模板复写。
- `多个 xx布包 / xx工具包 / xx防尘袋 / xx布袋` 的同后缀家族平移。
- 第四池重新滑回文书资料池的趋势。

## 复审结果

- 四池均为 50 条，池内唯一。
- `scene_expansion_items` 与其他三池无精确重复。
- `scene_expansion_items` 已通过当前中型场景碎片边界自检。
- 第四池中的抽象职能文书族已压到只剩 `图样词典` 1 个代表项，符合当前全局规则。
- 第四池中的 `布包 / 工具包 / 防尘袋 / 布袋` 已压到单个代表项，不再成串出现。

## 保留风险

- `手作+编织` 天然仍会有线团、针具、样片和册本密度，后续补写时仍要持续拦截“换材质不换本体”和“前缀换词、后缀不变”的模板复写。
- `visible_small_items` 仍有记号件、针具和样片单位，后续如再补写，优先补更厚实的小型材料单位，不要继续往扣、环、帽、夹上叠加。
- 第四池虽然已清掉记录本链，但后续仍要防止重新滑回文书资料池、工具包池和卷筒平移池。

## 自检记录

- 类内数量检查：通过
- 类内唯一性检查：通过
- 第四池跨池重叠检查：通过
- 第四池中型边界检查：通过
- 第四池抽象职能文书族数量检查：通过
- `blind_boxes.py` 语法检查：通过

## 最终复审记录

| 复审日期 | 复审人 | 复审结论 | 已解决问题 | 保留风险 |
| --- | --- | --- | --- | --- |
| 2026-05-07 | Codex | 通过 | 毛线包装链、针具链、办公漂移、洗护漂移、第四池壳体链已压降 | 后续仍需控制线团/针具/样片再次成片扩写 |
| 2026-05-08 | Codex | 再次通过 | 已清掉第四池 `展示架 / 展示车 / 推车 / 说明牌 / 展示台 / 模特架`，并同步压缩第二池的 `支架 / 托架 / 垫 / 针板 / 模板` | 册本、卷筒和工具包链仍需后续持续控密 |
| 2026-05-08 | Codex | 再次通过 | 已将第四池抽象职能文书族压到只剩 `图样词典` 1 个代表项，并将 `布包 / 工具包 / 防尘袋 / 布袋` 压到单个代表项 | 提篮、收纳篮和卷筒链仍需后续持续控密 |
