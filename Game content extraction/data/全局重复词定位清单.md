# 全局重复词定位清单

用途：记录 `Game content extraction/data/blind_boxes.py` 中跨类别高频重复的完整条目，方便统一修改与复查。

## 记录规则

- 统计范围：`BLIND_BOX_ITEM_POOL_BUNDLES` 四池完整条目。
- 纳入阈值：全库出现 `3 次及以上`。
- 位置写法：`类别 / 池名 / 条目 / blind_boxes.py:行号`
- 状态标签：`待修改`、`已修改`、`已复查`
- 本清单只记录“完整重名重复”，不代替同后缀家族审查。
- 非重复但不一眼成像、功能先于主体或解释成本过高的命名，改记 `全局不合理命名清单.md`。

## 当前快照

- 扫描日期：`2026-05-09`
- 扫描文件：`Game content extraction/data/blind_boxes.py`
- 统计结果：
  - `10 次及以上：0`
  - `5-9 次：0`
  - `4 次：0`
  - `3 次：0`
- 验证结果：`python -X utf8 "Game content extraction/test_blind_box_content_model.py"` 通过
- 当前结论：暂无需要纳入本清单的 `3 次及以上` 完整重复条目；非重复但仍明显抽象、流程化或不直观的命名，后续转入 `全局不合理命名清单.md` 统一处理。

## 后续约定

- 如果继续处理 `1 次组` 余项，优先盯“虽然不重复，但仍读起来像流程节点、标签词或工位说明词”的条目，而不是机械追求把某个后缀清零。
- 只要 `blind_boxes.py` 再次改动，行号就必须重新生成，不沿用旧定位。
