# Agents Guide

本仓库主体是中文提示词规则与输出格式维护；`Game content extraction/` 是配套的本地 `tkinter` 抽取工具。修改时优先局部编辑，保持中文风格、固定格式、数量配额、自检项和已建立的链路约束。

## 文件职责

- `主图 第二步 .md`：场景候选物审核与摆放；用户给出至少 1 个候选时，不得补全或猜测新候选。
- `组图 4.md`：找不同关卡润色；基于 Group 2 Image 设计 18 个 Group 3 差异点，九宫格每区 2 个，类型配额 3 / 6 / 3 / 3 / 3；禁用灯光、明暗、反光、高光、阴影等变化。
- `组图 23 表情库.md`：表情抽取资料源；正向 25 个、负向 25 个，每类 8 条眉/眼/嘴模板；1-4 单人，5-8 多人；只写面部，不改头身朝向、姿态、四肢、衣领、头发或耳饰。
- `组图 23 表情前置.md`：`组图 23.md` 上游；只输出负向组、正向组、交给组图 23 的摘要和自检；具体表情只写类别名，不输出完整眉/眼/嘴模板。
- `组图 23.md`：Ref-A / Ref-B / Target 差异迁移；绑定 Ref 真实可见变化，固定六板块；List 1 / List 2 各 16 条；维护时不得削弱 Ref 绑定、冷区避让、人物互斥、禁改规则和固定输出接口。
- `Game content extraction/`：本地中文抽取工具；主程序 `内容抽取.py`，静态内容在 `data/`，历史在 `draw_history.json`；保持现有盲盒/动物输入语法、历史机制和中文输出风格。
- `.workflow/.spec/SPEC-2026-04-20-game-content-extraction-人物表情抽取窗口/`：人物表情抽取窗口规格包；实现前优先读 `spec-summary.md`、`requirements/_index.md`、`architecture/_index.md`、`epics/_index.md`。

## 通用不变量

- 禁止脑补不可见对象、画外空间、遮挡后区域或身份不稳定对象。
- 差异点遵守“一圈一物一变化”或“单一不同点”。
- 不修改人物骨架、动作姿态、头身朝向、四肢动作或支撑关系。
- 不做灯光、发光、明暗、阴影、高光、反光、滤光、投影、镜面成像等变化。
- 不整体修改大面积背景、大物体主体或大面积表层。
- 位置字段只用于定位，不写颜色、材质、纹理、风格、明暗等外观修饰。
- 不把小变化升级成灾难、阴谋、案件、长期危机或大型事故。
- 中文文件保存为 UTF-8；命令行操作中文路径使用引号或 `-LiteralPath`。

## `组图 23` 链路

- 三段流程：`组图 23 表情前置.md` 产出正/负向剧情表情类别 -> App 按类别抽取具体眉/眼/嘴 -> `组图 23.md` 按剧情适配并生成完整差异方案。
- `组图 23.md` 只以 Ref-A -> Ref-B 的真实可见变化绑定事件功能；前置预案和用户表情描述只能参考，不得覆盖 Ref 真实性、Target 可见性、一圈一物一变化和禁改规则。
- 同一人物若使用面部表情区，不得再修改衣领/领带/颈部、头发/头饰、耳饰/耳侧饰品；若不改面部，才可选择这些附属区域。
- 单人表情眼部回指 `[证据物]` / `[目标物]`；多人表情眼部可回指 `[对方人物]`，但 `[对方人物]` 不得作为嘴部承载物。
- 嘴部承载物必须来自 Ref 明确可见且剧情需要的物体或状态；不得脑补来源、动作过程或额外剧情。
- `[剧情食物]`、`[剧情小物]` 只允许作为嘴部承载物出现在适合剧情证据的表情类别中。

## `Game content extraction`

- 小型 `tkinter` 桌面工具；不要默认改成 Web、数据库、服务端或大型多模块工程。
- 现有盲盒/动物抽取输入是逗号分隔单行语法；不得破坏多个盲盒编号、可选动物类型、`无某类`、`某类+N` / `某类-N`。
- `draw_history.json` 只服务现有物品池和动物池历史；人物表情抽取窗口第一版不得接入该历史降权机制。
- 人物表情抽取窗口规划：独立 `Toplevel`；读取 `组图 23 表情库.md`；按 `极性 + 具体表情 + 单人/多人` 查模板；1-4 单人，5-8 多人；支持指定模板编号。
- 表情抽取输出采用原文局部回填：只增强 `具体表情:` 字段，保留剧情、人物定位、表情功能、适配提示、禁用区域等原文内容。
- 第一版保留 `[目标物]`、`[证据物]`、`[对方人物]`、`[剧情食物]`、`[剧情小物]`，不自动替换，避免脑补。
- 验收样例：`极性=负向`、`具体表情=困惑`、`单人/多人=单人`、`模板编号=4` 应追加 `眉：一侧眉尾抬起，另一侧眉尾压平；眼：一侧眼撑开看着[目标物]，另一侧眼半垂；嘴：一侧嘴巴闭住下压，另一侧嘴角收紧。`

## 修改检查

- 修改前：读目标文件版本号、角色、输入规则、核心约束、输出格式；搜索同类规则，避免只改一处造成冲突。
- 修改中：只动直接相关段落；保留用户内容边界；不要新增“思维链”“候选池”“内部推理过程”等输出要求。
- 修改后：检查数量、格式、术语、优先级、禁区、承载关系、修改边界和自检是否一致。
- 涉及表情链路时，同步检查 `组图 23 表情库.md`、`组图 23 表情前置.md`、`组图 23.md` 的接入关系。
